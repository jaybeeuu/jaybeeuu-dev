import {  } from "../../paths";

export const update = async (): Promise<void> => {

  // get existing manifet
  // recurse files in repo
  //  create new ones and add to the manifest
  //  update new ones, update manifest.
  //  (old hashes get written into history map)


};