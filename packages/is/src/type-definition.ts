// import type { JSONSchema } from "json-schema-typed";

// const emit = (jsonSchema: JSONSchema[]): Promise<void> => {
//   for (const schema of jsonSchema) {
//     if (typeof schema === 'boolean') {
//       schema
//     } else {
//       schema.type =
//     }
//   }
// };

/**
 * WHat am I trying to build?
 *
 * JSON SChema as a vehicle to:
 *
 * * Documentation        -> ?
 * * Types                -> Choose Tool
 * * Validation           -> Choose Tool
 * * Test data Generation -> Find Tool
 */
