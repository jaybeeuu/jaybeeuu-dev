---
"@jaybeeuu/posts": major
---

Breaking: New package exports structure with `/config`, `/manifest`, and `/types` entry points. Adds compost configuration and typed manifest definitions. Requires `@jaybeeuu/compost` v2+.
